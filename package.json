{
  "name": "yadscache-fs",
  "version": "1.0.0",
  "description": "Node.js FS Adapter for yadscache",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel src --presets babel-preset-latest --out-dir dist",
    "prepublish": "npm run build"
  },
  "author": "Matthias Müller-Brockhausen",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0"
  },
  "dependencies": {
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "shelljs": "^0.7.5"
  }
}
